#include <iostream>
#include <algorithm>
using namespace std;
int main(int argc, char *argv[])
{
  int T;
  cin>>T;
  for(int i=0;i<T;i++){
    int N,D;
    int max_day=0;
    int temp;
    for(int j=0;j<N;j++){
      cin>>temp;
      max_day=max(max_day,D%temp);
    }
    cout<<"Case #"<<(i+1)<<": "<<D-max_day<<endl;
  }
  return 0;
}
