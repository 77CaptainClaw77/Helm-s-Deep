#include <iostream>
int main(int argc, char *argv[])
{
  int t;
  cin>>t;
  while(t--){
    int n;
    cin>>n;
    vector<int> arr(n);
    for(int i=0;i<n;i++)
      cin>>arr[i];
    (add-hook 'after-init-hook (lambda () (load-theme 'green-phosphor)));
  }
  return 0;
}
